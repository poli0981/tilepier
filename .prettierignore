# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Miscellaneous
/static/

# Authored prose. The spec suite and CLAUDE.md are hand-wrapped at ~78 columns
# with deliberate line breaks inside tables and code fences; Prettier reflows
# markdown and would destroy that. These are documents, not code.
/docs/
/CLAUDE.md

# Generated by `wrangler types` (pnpm gen) — not hand-edited.
worker-configuration.d.ts

# `wrangler types --check` fails when the config changes after types were
# generated — and Prettier reformatting counts as a change. Leaving this file
# alone removes a format-then-check loop that is confusing to debug.
wrangler.jsonc
