{
	"$comment": [
		"Bundle budgets, doc 20 §6. Single source consumed by",
		"scripts/check-budgets.mjs; the table in doc 20 §6 must match it.",
		"Limits are GZIPPED bytes unless the row says maxRawBytes.",
		"",
		"Chunks are identified by the SOURCE MODULE that produced them, read",
		"from the Vite/Rolldown client manifest — not by filename. SvelteKit",
		"owns the emitted names and they are content hashes, so a name-matching",
		"budget silently stops measuring the moment a hash changes.",
		"",
		"Raising a number is a conscious decision that belongs in the same PR as",
		"the doc 20 §6 edit."
	],
	"budgets": [
		{
			"id": "entry",
			"label": "Entry (shell + registry + tier-1 essentials)",
			"kind": "entry-js",
			"maxGzipBytes": 204800
		},
		{
			"id": "css",
			"label": "CSS total",
			"kind": "css-total",
			"maxGzipBytes": 46080
		},
		{
			"id": "fonts",
			"label": "Fonts total (both families, subsets)",
			"kind": "static-glob",
			"glob": "fonts/*.woff2",
			"maxRawBytes": 225280
		},
		{
			"id": "echarts",
			"label": "Shared echarts core chunk (lazy, counted once)",
			"kind": "module",
			"module": "^src/lib/charts/echarts\\.ts$",
			"maxGzipBytes": 337920
		},
		{
			"id": "maplibre",
			"label": "maplibre chunk (map detail only)",
			"kind": "module",
			"module": "maplibre-gl",
			"maxGzipBytes": 307200
		},
		{
			"id": "widget-tile",
			"label": "Each widget tile chunk",
			"kind": "module-each",
			"module": "^src/lib/widgets/[^/]+/Tp[A-Za-z]+Widget\\.svelte$",
			"maxGzipBytes": 40960,
			"optional": true
		},
		{
			"id": "detail",
			"label": "Detail chunks (excluding the shared echarts chunk)",
			"kind": "module-each",
			"module": "^src/lib/widgets/[^/]+/Tp[A-Za-z]+Detail\\.svelte$",
			"maxGzipBytes": 358400,
			"optional": true
		}
	]
}
