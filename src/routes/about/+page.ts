/**
 * Prerendered and outside the `(app)` group, next to /legal/* (doc 13 §11):
 * the gate links here, so it has to be readable before acceptance.
 */
export const prerender = true;
export const ssr = true;
